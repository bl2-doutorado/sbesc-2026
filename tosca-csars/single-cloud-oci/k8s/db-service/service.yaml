apiVersion: v1
kind: Service
metadata:
  name: ${env_name}-db
  labels:
    app.kubernetes.io/name: db
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: db