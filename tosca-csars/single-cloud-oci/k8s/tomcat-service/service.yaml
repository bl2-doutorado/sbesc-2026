apiVersion: v1
kind: Service
metadata:
  name: tomcat-service
  namespace: default
  labels:
    app: tomcat
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: tomcat
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
