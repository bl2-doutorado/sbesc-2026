apiVersion: v1
kind: Service
metadata:
  name: ${env_name}-backend
  labels:
    app.kubernetes.io/name: backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: backend