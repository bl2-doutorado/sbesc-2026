apiVersion: v1
kind: Service
metadata:
  name: ${env_name}-adminer
  labels:
    app.kubernetes.io/name: adminer
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: adminer